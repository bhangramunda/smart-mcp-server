# Railway Environment Variables for MCP Server

# Railway automatically sets PORT
PORT=3001

# Node Environment
NODE_ENV=production

# Default Sitecore connection settings (can be overridden by API calls)
SITECORE_INSTANCE_URL=https://cm-qa-sc103.kajoo.ca
SITECORE_USERNAME=admin
SITECORE_PASSWORD=b
SITECORE_DATABASE=master

# Optional: Sitecore API Key (if using token authentication)
# SITECORE_API_KEY=your-api-key-here

# Optional: GraphQL endpoint (auto-constructed if not provided)
# SITECORE_GRAPHQL_ENDPOINT=https://cm-qa-sc103.kajoo.ca/sitecore/api/graph/edge